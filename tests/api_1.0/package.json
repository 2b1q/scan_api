{
  "name": "BANKEX-Scan-API-testing",
  "version": "1.0.0",
  "description": "BANKEX scan Backend REST API testing",
  "dependencies": {
    "body-parser": "^1.17.1",
    "chai": "^3.5.0",
    "mocha": "^5.2.0",
    "mocha-jenkins-reporter": "^0.3.7",
    "npm": "^6.2.0",
    "supertest": "^3.0.0"
  },
  "scripts": {
    "//prestart": "npm install;",
    "start": "JUNIT_REPORT_PATH=test_result/result.xml mocha --timeout 25000 --colors --reporter mocha-jenkins-reporter || true",
    "//start": "JUNIT_REPORT_PATH=test_result/result.xml JUNIT_REPORT_STACK=1 mocha --timeout 25000 --colors --reporter mocha-jenkins-reporter || true"
  }
}
